/* 
# What is an Array?
=> Array is an object that lets to store Multiple value in a single variable.
=> Length of a Javascript array and the types of its element are not fixed.
=> Array can use integers as element indexs.

*/

//Simple Array.

let cars = ['Toyota', 'Nissan', 'Land Rover', 'BMW'];
document.write(cars);

document.write(`\n`);

//Array Oprational

let BMW = cars[0];
document.write(BMW);


let len = cars.length;
document.getElementById('demo')